name: Deploy ST-Move-files-for-Salesforce-CRM (sandbox)

on:
  push:
    branches: [ sandbox ]
    paths:
      - 'ST-Move-files-for-Salesforce-CRM/**'
      - '.github/workflows/deploy-ST-Move-files-for-Salesforce-CRM.yml'
  workflow_dispatch: {}

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Ensure zip and unzip are installed
        run: |
          if ! command -v zip >/dev/null 2>&1; then
            sudo apt-get update
            sudo apt-get install -y zip unzip
          fi

      - name: Create deployment package
        working-directory: ./ST-Move-files-for-Salesforce-CRM
        run: |
          zip -r ../ST-Move-files-for-Salesforce-CRM.zip .

      - name: List files in deployment package
        run: unzip -l ST-Move-files-for-Salesforce-CRM.zip

      - name: Update Lambda function code
        uses: imehedi/actions-awscli-v2@latest
        with:
          args: >
            lambda update-function-code
            --function-name arn:aws:lambda:ca-central-1:839358208765:function:ST-Move-files-for-Salesforce-CRM
            --zip-file fileb://ST-Move-files-for-Salesforce-CRM.zip
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: ca-central-1